@using System.ComponentModel
@{
    var convertor = TypeDescriptor.GetConverter(Model);
    string value;
    if (convertor?.CanConvertTo(typeof(string)) ?? false)
    {
        value = convertor.ConvertToString(Model);
    }
    else
    {
        value = Model.ToString();
    }
}
@value
